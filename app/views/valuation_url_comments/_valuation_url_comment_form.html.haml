= simple_form_for comment, remote: true,
                  url: valuation_url_comments_path(id: valuation_url.id, format: :js) do |f|
  = f.input :comment

  .center-text
    = f.submit nil, class: "btn btn-primary",
             data: { disable_with: "Submitting ..." }
