@font-face {
  font-family: 'impactreg';
  src: url(<%= Rails.root.join('app','assets','fonts', 'impactreg.eot') %>);
  src: url(<%= Rails.root.join('app','assets','fonts', 'impactreg.woff') %>) format('woff'), url(<%= Rails.root.join('app','assets','fonts', 'impactreg.ttf') %>) format('truetype');
}
